cmake_minimum_required(VERSION 2.8)
project( stereopostocv )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( stereopostocv main.cpp)
target_link_libraries( stereopostocv ${OpenCV_LIBS} )